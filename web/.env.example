# A interface local já conversa com esta URL automaticamente.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Na publicação, defina como false. A chave de dados individuais deve ficar
# exclusivamente nas variáveis privadas do servidor.
VITE_ENABLE_LOCAL_KEY_SETUP=true
